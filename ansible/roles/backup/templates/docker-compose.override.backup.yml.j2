# Autogenerated by vff-backup role — do not edit
services:
{% for container, dump_dir in db_containers | zip(dump_dirs) %}
  {{ container }}:
    volumes:
      - "{{ dump_dir }}:{{ dump_dir }}:rw"
{% endfor %}
